# SupportEngineerExcercises

Per eseguire lo scritp digitare da riga di comando:

    python {stringToFind} {stringToReplace} {folderPath}

sostituendo {stringToFind} {stringToReplace} {folderPath} con i parametri desiderati.
